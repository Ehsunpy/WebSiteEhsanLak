# وب‌سایت شخصی احسان لک

وب‌سایت پورتفولیو، بلاگ و دوره‌های آموزشی با Django

## ویژگی‌ها

- 🎨 طراحی مدرن با انیمیشن‌های اسکرول جذاب
- 📝 سیستم بلاگ با دسته‌بندی و تگ
- 🎓 پلتفرم دوره‌های آموزشی با آپلود فایل (PDF, Word, تصویر, ویدیو)
- 👤 سیستم احراز هویت (ایمیل + ورود با Google)
- 📱 طراحی واکنش‌گرا (Responsive)
- 🌐 پشتیبانی کامل از زبان فارسی

## نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.11 یا بالاتر
- pip
- virtualenv (اختیاری اما توصیه می‌شود)

### مراحل نصب

1. **کلون یا دانلود پروژه**

```bash
cd /home/ehsan/Documents/EHSAN-WEB
```

2. **ایجاد و فعال‌سازی محیط مجازی (اگر ندارید)**

```bash
python -m venv .venv
source .venv/bin/activate  # در Linux/Mac
# یا
.venv\Scripts\activate  # در Windows
```

3. **نصب وابستگی‌ها**

```bash
pip install -r requirements.txt
```

4. **اجرای مایگریشن‌های دیتابیس**

```bash
python manage.py migrate
```

5. **ایجاد اکانت ادمین (Superuser)**

```bash
python manage.py createsuperuser
```

سپس اطلاعات زیر را وارد کنید:
- Username: (نام کاربری دلخواه)
- Email: (ایمیل شما)
- Password: (رمز عبور قوی)

6. **اجرای سرور توسعه**

```bash
python manage.py runserver
```

سایت در آدرس `http://127.0.0.1:8000` در دسترس خواهد بود.

## دسترسی به پنل مدیریت

### ورود به پنل ادمین

1. سرور را اجرا کنید: `python manage.py runserver`
2. به آدرس زیر بروید: `http://127.0.0.1:8000/admin/`
3. با نام کاربری و رمز عبوری که در مرحله `createsuperuser` ساختید وارد شوید

### بخش‌های پنل مدیریت

در پنل ادمین می‌توانید مدیریت کنید:

- **Core**: فعالیت‌ها، تجربیات، مهارت‌ها، دستاوردها
- **Blog**: دسته‌بندی‌ها و پست‌های بلاگ
- **Courses**: دسته‌بندی‌ها و دوره‌های آموزشی
- **Accounts**: پروفایل کاربران
- **Users**: مدیریت کاربران سیستم

## ساختار پروژه

```
EHSAN-WEB/
├── core/              # صفحات اصلی (خانه، درباره من، تماس)
├── blog/              # سیستم بلاگ
├── courses/           # سیستم دوره‌های آموزشی
├── accounts/          # مدیریت کاربران
├── templates/         # فایل‌های HTML
├── static/            # فایل‌های CSS, JS, تصاویر
├── media/             # فایل‌های آپلود شده کاربران
├── ehsan_site/        # تنظیمات اصلی پروژه
└── manage.py          # فایل مدیریت Django
```

## تنظیمات مهم

### تنظیمات ایمیل (برای فرم تماس)

در فایل `ehsan_site/settings.py` می‌توانید تنظیمات SMTP را اضافه کنید:

```python
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your-email@gmail.com'
EMAIL_HOST_PASSWORD = 'your-app-password'
```

### تنظیمات Google OAuth (برای ورود با Google)

1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. یک پروژه جدید ایجاد کنید
3. OAuth 2.0 Client ID ایجاد کنید
4. در فایل `.env` یا `settings.py` اضافه کنید:

```python
SOCIALACCOUNT_PROVIDERS = {
    'google': {
        'APP': {
            'client_id': 'YOUR_CLIENT_ID',
            'secret': 'YOUR_CLIENT_SECRET',
            'key': ''
        }
    }
}
```

## دستورات مفید

```bash
# ایجاد مایگریشن جدید
python manage.py makemigrations

# اعمال مایگریشن‌ها
python manage.py migrate

# جمع‌آوری فایل‌های استاتیک
python manage.py collectstatic

# اجرای تست‌ها
python manage.py test

# دسترسی به shell Django
python manage.py shell
```

## اطلاعات تماس

- **نام**: احسان لک
- **شماره تماس**: +98 937 054 6885
- **موقعیت**: تهران، ایران

## لایسنس

این پروژه برای استفاده شخصی ساخته شده است.

