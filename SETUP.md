# راهنمای سریع نصب و ورود به پنل مدیریت

## 🚀 نصب سریع

```bash
# 1. فعال‌سازی محیط مجازی
source .venv/bin/activate

# 2. نصب وابستگی‌ها
pip install -r requirements.txt

# 3. اجرای مایگریشن‌ها
python manage.py migrate

# 4. ایجاد اکانت ادمین
python manage.py createsuperuser
```

هنگام اجرای `createsuperuser` از شما خواسته می‌شود:
- **Username**: یک نام کاربری وارد کنید (مثلاً: `admin` یا `ehsan`)
- **Email address**: ایمیل خود را وارد کنید
- **Password**: یک رمز عبور قوی انتخاب کنید (حداقل 8 کاراکتر)

## 🔐 ورود به پنل مدیریت

1. **اجرای سرور:**
   ```bash
   python manage.py runserver
   ```

2. **باز کردن مرورگر:**
   - به آدرس زیر بروید: `http://127.0.0.1:8000/admin/`
   - یا: `http://localhost:8000/admin/`

3. **وارد شدن:**
   - Username: همان نام کاربری که در `createsuperuser` وارد کردید
   - Password: همان رمز عبوری که انتخاب کردید

## 📋 چک‌لیست اولیه

بعد از ورود به پنل، این موارد را اضافه کنید:

### ✅ در بخش Core:
- [ ] یک یا چند **Activity Spotlight** اضافه کنید (فعالیت‌های برجسته)
- [ ] **Experience** اضافه کنید (تجربیات کاری/تحصیلی)
- [ ] **Skill** اضافه کنید (مهارت‌ها)
- [ ] **Recognition** اضافه کنید (دستاوردها)
- [ ] **Metric Badge** اضافه کنید (آمار و اعداد)

### ✅ در بخش Blog:
- [ ] یک یا چند **Post Category** ایجاد کنید (دسته‌بندی‌های بلاگ)
- [ ] **Post** اضافه کنید (پست‌های بلاگ)

### ✅ در بخش Courses:
- [ ] یک یا چند **Course Category** ایجاد کنید
- [ ] **Course** اضافه کنید (دوره‌های آموزشی)
- [ ] برای هر دوره **Course Resource** اضافه کنید (فایل‌های PDF, Word, تصویر, ویدیو)

## 🎨 مشاهده سایت

بعد از اضافه کردن محتوا، به آدرس زیر بروید:
- صفحه اصلی: `http://127.0.0.1:8000/`
- بلاگ: `http://127.0.0.1:8000/blog/`
- دوره‌ها: `http://127.0.0.1:8000/courses/`
- درباره من: `http://127.0.0.1:8000/about/`
- تماس: `http://127.0.0.1:8000/contact/`

## ⚠️ نکات مهم

- اگر superuser را فراموش کردید، می‌توانید دوباره اجرا کنید:
  ```bash
  python manage.py createsuperuser
  ```
  (اگر نام کاربری تکراری باشد، از شما رمز جدید می‌خواهد)

- برای تغییر رمز یک کاربر:
  ```bash
  python manage.py changepassword <username>
  ```

- فایل‌های آپلود شده در پوشه `media/` ذخیره می‌شوند

## 🆘 مشکل دارید؟

اگر مشکلی پیش آمد:
1. مطمئن شوید محیط مجازی فعال است
2. همه وابستگی‌ها نصب شده باشند: `pip install -r requirements.txt`
3. مایگریشن‌ها اجرا شده باشند: `python manage.py migrate`
4. سرور در حال اجرا باشد: `python manage.py runserver`

